# NOTE: this file must stay UTF-8. It was previously committed as UTF-16LE,
# which pip cannot parse.

# The `+cpu` build is served only from PyTorch's own index. Pinning it there
# avoids the default PyPI wheel, which bundles CUDA and is several times larger
# for no benefit -- this app runs inference on CPU. Deploy targets are Linux
# and Windows; on macOS install plain `torch==2.13.0` from PyPI instead, which
# is already CPU-only.
--extra-index-url https://download.pytorch.org/whl/cpu

streamlit==1.61.1

# Primary PDF backend: ~60x faster than pdfplumber on text extraction.
pypdfium2==5.13.0
# Retained as the fallback for documents pdfium reads as empty.
pdfplumber==0.11.10

nltk==3.10.3
torch==2.13.0+cpu
transformers==5.15.0

# Chunk retrieval is BM25 in pure Python (retrieval.py) -- no dependency, no
# model download. Static embeddings were benchmarked against it and lost.
